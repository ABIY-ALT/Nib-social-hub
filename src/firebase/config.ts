'use client';

// This is a placeholder for your Firebase config.
// In a real-world scenario, you would replace this with your actual
// Firebase project configuration from the Firebase console.
// It's recommended to use environment variables to store this information.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID",
};
